# Cloudflare Pages + Workers config for advanseit.com.au
name = "advanseit-main"
compatibility_date = "2024-11-01"

[site]
bucket = "./dist/public"

# Build command: pnpm install && pnpm run build
# Output directory: dist/public
